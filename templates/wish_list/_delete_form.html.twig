<form method="post" action="{{ path('app_wish_list_delete', {'slug': wish_list.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wish_list.slug) }}">
    <button class="btn">Delete</button>
</form>
