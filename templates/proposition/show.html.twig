{% extends 'base.html.twig' %}

{% block title %}Proposition{% endblock %}

{% block body %}
    <div class="with-aside">
        <aside class="aside-left">
            <p><a href="{{ path('app_proposition_changelabels', {id: proposition.id}) }}" class="button">Changer les étiquettes</a></p>

            <p><a href="{{ path('app_proposition_changestate', {id: proposition.id}) }}" class="button">Changer l'état</a></p>

            <p><a href="{{ path('app_proposition_edit', {'id': proposition.id}) }}" class="button">Editer la proposition</a></p>

            <p>{{ include('proposition/_delete_form.html.twig') }}</p>

            <p><a href="{{ path('app_wish_list_show', {slug: proposition.wishlist.slug}) }}" class="button">Retour à la liste</a></p>
        </aside>
        <main class="main">
            <h1>Proposition</h1>

           {% include 'components/_proposition.html.twig' %}

            {% include 'components/_proposition_comments.html.twig' %}

            <p><a href="{{ path('app_add_comment', {id: proposition.id}) }}" class="button comment">Ajouter un commentaire</a></p>



        </main>
    </div>
{% endblock %}
